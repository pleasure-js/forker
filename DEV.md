# Build the project

```sh
$ yarn build
```

# Build on change

Using `nodemon` to monitor changes.

```sh
$ yarn build:live
```

# Running DaemonServer

```sh
# from package root
$ DAEMONIZER_DAEMON_START=true node .
```
